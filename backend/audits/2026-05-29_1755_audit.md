<!-- Generated by Claude Code on 2026-05-29T17:55:50+00:00 -->

Output matches the requested format. All validations pass and `/opt/imperium-backend` was not touched.

### Fichiers modifiés
- `/opt/orchestrator/asset_registry.py` — ajout `GLOBAL_INVENTORY_APP_ORDER` + `build_global_asset_inventory()` (réutilise `build_asset_inventory()` et strip de son en-tête par app).
- `/opt/orchestrator/telegram_bot.py` — bouton `🌍 Inventaire global` en première ligne de `_inventory_app_keyboard()`, dispatch `assets:inventory:global` placé avant le prefix générique, handler `_handle_inventory_global()` calqué sur `_handle_inventory_app()` (mêmes `_send_chunks`, mêmes garde-fous d'exception, mêmes retours clavier).

### Format final produit
```
GLOBAL ASSET INVENTORY

====================
IMPERIUM

(aucun pack)

====================
VAULT

Pack 20260529T162943Z
layout: 2x2
status: named
- financial_pressure_gauge
- monthly_profit_ring
- cashflow_progress_bar
- savings_goal_tracker

====================
VECTOR

(aucun pack)

====================
PULSE

(aucun pack)

====================
PATH

Pack 20260529T144805Z
layout: 2x3
status: unnamed
- asset_01.png
…
```
- Ordre apps : Imperium → Vault → Vector → Pulse → Path ✅
- Packs newest-first (hérité de `build_asset_inventory()`) ✅
- `_send_chunks` réutilisé ✅

### Validations exécutées
- `python3 -m py_compile main.py telegram_bot.py asset_registry.py asset_naming.py design_review.py state.py config.py` → `PYCOMPILE OK`
- `grep -Rni ... "Inventaire global|assets:inventory:global|build_global_asset_inventory"` → 7 hits attendus dans `asset_registry.py` + `telegram_bot.py`, rien ailleurs.
- Smoke test : `build_global_asset_inventory()` exécuté avec succès, format conforme.

### Sécurité
- Whitelist `ALLOWED_APPS` réutilisée via `build_asset_inventory()` (chaque app passe par `is_known_app()`).
- `Path.resolve()` et le containment `_is_under(..., ASSETS_REGISTRY_DIR)` hérités sans changement.
- Aucune écriture, aucun rename, aucun move, aucun delete.

### Compatibilité
- ORCH-11.2 intact : `_handle_inventory_app`, `assets:inventory:open`, signatures inchangées.
- ZIP / Naming / Design Review / CodeDesign : aucun fichier ni handler concerné modifié.
- Backend Imperium intact : aucune modification hors `/opt/orchestrator` (confirmé visuellement et par périmètre des Edit).
- Aucun appel DB ni git.