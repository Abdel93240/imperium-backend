<!-- Generated by Claude Code on 2026-05-28T08:04:17+00:00 -->

All checks pass. Summary:

**Fichiers modifiés**
- `/opt/orchestrator/image_runner.py`

**IDs finaux**
- `flux_pro` → `black-forest-labs/flux.2-pro`
- `flux_kontext` → `black-forest-labs/flux.2-max` (max = remplacement le plus proche de Kontext Pro, plus haut de gamme que flex pour génération image)

**Endpoint**
- Conservé : `POST {base}/chat/completions` avec `modalities: ["image", "text"]` (forme OpenRouter actuelle pour génération image — pas de `/images/generations` dans le code, aucune migration nécessaire). `send_photo` et l'écriture fichier sont inchangés.

**Hygiène erreurs / logs**
- Nouveau `_sanitize_error_body` : parse le JSON OpenRouter, extrait `error.message`, masque `user_id=***xxxx` via `_USER_ID_RE`, tronque à 240 chars. Le stderr renvoyé devient `OpenRouter HTTP 400: <message court>` au lieu du body brut.
- Logs inchangés : aucun header `Authorization`, pas d'API key, pas de body base64 ; seul `body_chars` est journalisé.

**Validation**
```
$ python3 -m py_compile image_runner.py telegram_bot.py
OK_COMPILE
$ grep -n "flux.2\|images/generations\|chat/completions\|modalities" image_runner.py
5:    [Flux Pro]      → black-forest-labs/flux.2-pro
6:    [Flux Kontext]  → black-forest-labs/flux.2-max
43:    "flux_pro": "black-forest-labs/flux.2-pro",
44:    "flux_kontext": "black-forest-labs/flux.2-max",
181:    POST /chat/completions on OpenRouter with image modality...
207:    url = f"{base_url}/chat/completions"
215:        "modalities": ["image", "text"],
```

Aucun fichier de `/opt/imperium-backend` n'a été touché.