<!-- Generated by Claude Code on 2026-05-28T07:46:03+00:00 -->

Both validations pass. The `[🎨 Image]` button is wired in `telegram_bot.py:67` (3rd row of the main keyboard), with callback `image:open` opening the `[Flux Pro] [Flux Kontext]` sub-keyboard (`telegram_bot.py:74-79`), and the `image:flux_pro` / `image:flux_kontext` callbacks set `pending_image_model` so the next user text becomes the image prompt (`telegram_bot.py:184-198`, `telegram_bot.py:317-324`, `telegram_bot.py:601-666`).

**Files actually modified / created:**
- `/opt/orchestrator/image_runner.py` (new) — `run_image()` using `httpx.AsyncClient`, 180s timeout, `OPENROUTER_API_KEY` via `config.settings`, saves to `generated_images/{ts}_{alias}_{slug}_{uuid8}.png`. Never logs key, Authorization, base64, or URL body.
- `/opt/orchestrator/telegram_bot.py` — added `import image_runner`, `[🎨 Image]` row in `_main_keyboard()`, new `_image_keyboard()`, `image:open` + `image:flux_pro|flux_kontext` branches in `on_button`, pending-image branch in `on_prompt`, and `_run_image_and_report()` which calls `bot.send_photo`.
- `/opt/orchestrator/state.py` — added `pending_image_model: str | None = None`.
- `/opt/orchestrator/.gitignore` — appended `generated_images/`.

**Commands executed:**
- `python3 -m py_compile main.py telegram_bot.py image_runner.py config.py` → `OK_PYCOMPILE`
- `grep -Rni --exclude-dir=venv --exclude-dir=__pycache__ "image:flux\|generated_images\|send_photo\|image_runner" . | head -120` → 11 hits (output above).

**Confirmation:** the Image button is wired into `telegram_bot.py` — main keyboard row 3 → `image:open` → sub-keyboard with `image:flux_pro` / `image:flux_kontext` → next user message is consumed as the image prompt and dispatched to `image_runner.run_image()`. Existing ORCH-4 text routing (`model:*`, `reasoning:*`, `audit:*`, `limit:*`) is untouched. No DB writes, no git push, no changes to `openrouter_runner.py` or to `/opt/imperium-backend`.